spring.application.name=blooddonation

# ✅ Uses environment variables when deployed, falls back to localhost for dev
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/blood_donation}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Premkumar7099}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ✅ Allow CORS for deployed frontend
server.port=${PORT:8080}